# JavaScript array findIndex() method

# How to use findIndex() method:

    // normal function;
    array.findIndex(function(element, index, array){

        // statement;

    }, this);


    // arrow function;
    array.findIndex((element, index, array){

        // statement;

    });
